#' CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
#'
#' CryptoCompare is the world's leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
#'
#' The version of the OpenAPI document: 2.1.1
#' Contact: data@cryptocompare.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Overview operations
#' @description OverviewApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ OverviewV1HistoricalMarketcapAllAssetsDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OverviewV1HistoricalMarketcapAllAssetsHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OverviewV1HistoricalMarketcapFtwAssetsDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OverviewV1HistoricalMarketcapFtwAssetsHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OverviewV1LatestMarketcapAllTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OverviewV1LatestMarketcapFtwTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  OverviewV1HistoricalMarketcapAllAssetsDays  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1HistoricalMarketcapAllAssetsDays(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$OverviewV1HistoricalMarketcapAllAssetsDays(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  OverviewV1HistoricalMarketcapAllAssetsHours  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1HistoricalMarketcapAllAssetsHours(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$OverviewV1HistoricalMarketcapAllAssetsHours(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  OverviewV1HistoricalMarketcapFtwAssetsDays  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1HistoricalMarketcapFtwAssetsDays(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$OverviewV1HistoricalMarketcapFtwAssetsDays(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  OverviewV1HistoricalMarketcapFtwAssetsHours  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1HistoricalMarketcapFtwAssetsHours(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$OverviewV1HistoricalMarketcapFtwAssetsHours(groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  OverviewV1LatestMarketcapAllTick  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting all market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1LatestMarketcapAllTick(groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$OverviewV1LatestMarketcapAllTick(groups = var_groups)
#' dput(result)
#'
#'
#' ####################  OverviewV1LatestMarketcapFtwTick  ####################
#'
#' library(openapi)
#' var_groups <- c("inner_example") # array[character] | When requesting ftw universe market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (Optional)
#'
#' api_instance <- OverviewApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OverviewV1LatestMarketcapFtwTick(groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$OverviewV1LatestMarketcapFtwTick(groups = var_groups)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OverviewApi <- R6::R6Class(
  "OverviewApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OverviewApi.
    #'
    #' @description
    #' Initialize a new OverviewApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1HistoricalMarketcapAllAssetsDays = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$OverviewV1HistoricalMarketcapAllAssetsDaysWithHttpInfo(groups, limit, to_ts, aggregate, fill, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1HistoricalMarketcapAllAssetsDaysWithHttpInfo = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsDays, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsDays, must be bigger than or equal to 1.")
      }



      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/overview/v1/historical/marketcap/all/assets/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1HistoricalMarketcapAllAssetsHours = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$OverviewV1HistoricalMarketcapAllAssetsHoursWithHttpInfo(groups, limit, to_ts, aggregate, fill, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1HistoricalMarketcapAllAssetsHoursWithHttpInfo = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsHours, must be bigger than or equal to 1.")
      }



      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling OverviewApi$OverviewV1HistoricalMarketcapAllAssetsHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/overview/v1/historical/marketcap/all/assets/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1HistoricalMarketcapFtwAssetsDays = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$OverviewV1HistoricalMarketcapFtwAssetsDaysWithHttpInfo(groups, limit, to_ts, aggregate, fill, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1HistoricalMarketcapFtwAssetsDaysWithHttpInfo = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsDays, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsDays, must be bigger than or equal to 1.")
      }



      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/overview/v1/historical/marketcap/ftw/assets/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1HistoricalMarketcapFtwAssetsHours = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$OverviewV1HistoricalMarketcapFtwAssetsHoursWithHttpInfo(groups, limit, to_ts, aggregate, fill, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1HistoricalMarketcapFtwAssetsHoursWithHttpInfo = function(groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsHours, must be bigger than or equal to 1.")
      }



      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling OverviewApi$OverviewV1HistoricalMarketcapFtwAssetsHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/overview/v1/historical/marketcap/ftw/assets/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1LatestMarketcapAllTick = function(groups = [], data_file = NULL, ...) {
      local_var_response <- self$OverviewV1LatestMarketcapAllTickWithHttpInfo(groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting all market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1LatestMarketcapAllTickWithHttpInfo = function(groups = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/overview/v1/latest/marketcap/all/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    OverviewV1LatestMarketcapFtwTick = function(groups = [], data_file = NULL, ...) {
      local_var_response <- self$OverviewV1LatestMarketcapFtwTickWithHttpInfo(groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param groups (optional) When requesting ftw universe market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    OverviewV1LatestMarketcapFtwTickWithHttpInfo = function(groups = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/overview/v1/latest/marketcap/ftw/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
