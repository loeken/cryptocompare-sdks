#' CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
#'
#' CryptoCompare is the world's leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
#'
#' The version of the OpenAPI document: 2.1.1
#' Contact: data@cryptocompare.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title News operations
#' @description NewsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ NewsV1ArticleList } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } source_ids list( character )
#' \item \emph{ @param } lang Enum < [EN, ES, PT, ] >
#' \item \emph{ @param } categories Enum < [1INCH, AAVE, ADA, ALGO, ALTCOIN, ASIA, ATOM, AVAX, AXS, BAL, BAT, BCH, BLOCKCHAIN, BTC, BUSINESS, COMMODITY, COMP, CRV, DASH, DOGE, DOT, ENJ, ETC, ETH, EXCHANGE, FIAT, FIL, FTM, ICO, KNC, LINK, LRC, LTC, LUNA, LUNC, MANA, MARKET, MATIC, MINING, MKR, REGULATION, REN, SAND, SHIB, SNX, SOL, SPONSORED, SUSHI, TECHNOLOGY, TRADING, TRX, UMA, UNI, USDT, WALLET, XLM, XMR, XRP, XTZ, YFI, ZEC, ZRX, ] >
#' \item \emph{ @param } exclude_categories Enum < [1INCH, AAVE, ADA, ALGO, ALTCOIN, ASIA, ATOM, AVAX, AXS, BAL, BAT, BCH, BLOCKCHAIN, BTC, BUSINESS, COMMODITY, COMP, CRV, DASH, DOGE, DOT, ENJ, ETC, ETH, EXCHANGE, FIAT, FIL, FTM, ICO, KNC, LINK, LRC, LTC, LUNA, LUNC, MANA, MARKET, MATIC, MINING, MKR, REGULATION, REN, SAND, SHIB, SNX, SOL, SPONSORED, SUSHI, TECHNOLOGY, TRADING, TRX, UMA, UNI, USDT, WALLET, XLM, XMR, XRP, XTZ, YFI, ZEC, ZRX, ] >
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ NewsV1CategoryList } \emph{  }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ NewsV1SourceList } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } lang Enum < [EN, ES, PT, ] >
#' \item \emph{ @param } type Enum < [RSS, API, TWITTER, ] >
#' \item \emph{ @param } status Enum < [ACTIVE, INACTIVE, ] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  NewsV1ArticleList  ####################
#'
#' library(openapi)
#' var_source_ids <- c("inner_example") # array[character] | Get articles from specific sources; either by their ids or keys (Optional)
#' var_lang <- "EN" # character | The article Preferred language - English (EN), Portuguese (PT), Espanol (ES) (Optional)
#' var_categories <- c("1INCH") # array[character] | News article categories to return (Optional)
#' var_exclude_categories <- c("1INCH") # array[character] | News article categories to exclude from results (Optional)
#' var_to_ts <- -1 # integer | Articles published on or before this timestamp (Optional)
#'
#' api_instance <- NewsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$NewsV1ArticleList(source_ids = var_source_ids, lang = var_lang, categories = var_categories, exclude_categories = var_exclude_categories, to_ts = var_to_tsdata_file = "result.txt")
#' result <- api_instance$NewsV1ArticleList(source_ids = var_source_ids, lang = var_lang, categories = var_categories, exclude_categories = var_exclude_categories, to_ts = var_to_ts)
#' dput(result)
#'
#'
#' ####################  NewsV1CategoryList  ####################
#'
#' library(openapi)
#'
#' api_instance <- NewsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$NewsV1CategoryList(data_file = "result.txt")
#' result <- api_instance$NewsV1CategoryList()
#' dput(result)
#'
#'
#' ####################  NewsV1SourceList  ####################
#'
#' library(openapi)
#' var_lang <- "EN" # character | The article source Preferred language - English (EN), Portuguese (PT), Espanol (ES) (Optional)
#' var_type <- "RSS" # character | RSS, API, TWITTER (Optional)
#' var_status <- "ACTIVE" # character | The status for the  article source entry. Allowed values: ACTIVE, INACTIVE (Optional)
#'
#' api_instance <- NewsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$NewsV1SourceList(lang = var_lang, type = var_type, status = var_statusdata_file = "result.txt")
#' result <- api_instance$NewsV1SourceList(lang = var_lang, type = var_type, status = var_status)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
NewsApi <- R6::R6Class(
  "NewsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new NewsApi.
    #'
    #' @description
    #' Initialize a new NewsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param source_ids (optional) Get articles from specific sources; either by their ids or keys (default value: [""])
    #' @param lang (optional) The article Preferred language - English (EN), Portuguese (PT), Espanol (ES) (default value: "EN")
    #' @param categories (optional) News article categories to return (default value: [""])
    #' @param exclude_categories (optional) News article categories to exclude from results (default value: [""])
    #' @param to_ts (optional) Articles published on or before this timestamp (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    NewsV1ArticleList = function(source_ids = [""], lang = "EN", categories = [""], exclude_categories = [""], to_ts = -1, data_file = NULL, ...) {
      local_var_response <- self$NewsV1ArticleListWithHttpInfo(source_ids, lang, categories, exclude_categories, to_ts, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param source_ids (optional) Get articles from specific sources; either by their ids or keys (default value: [""])
    #' @param lang (optional) The article Preferred language - English (EN), Portuguese (PT), Espanol (ES) (default value: "EN")
    #' @param categories (optional) News article categories to return (default value: [""])
    #' @param exclude_categories (optional) News article categories to exclude from results (default value: [""])
    #' @param to_ts (optional) Articles published on or before this timestamp (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    NewsV1ArticleListWithHttpInfo = function(source_ids = [""], lang = "EN", categories = [""], exclude_categories = [""], to_ts = -1, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (nchar(`lang`) < 1) {
        stop("Invalid length for `lang` when calling NewsApi$NewsV1ArticleList, must be bigger than or equal to 1.")
      }

      if (length(`categories`) > 200) {
        stop("Invalid length for `categories` when calling NewsApi$NewsV1ArticleList, number of items must be less than or equal to 200.")
      }

      if (length(`exclude_categories`) > 200) {
        stop("Invalid length for `exclude_categories` when calling NewsApi$NewsV1ArticleList, number of items must be less than or equal to 200.")
      }


      # no explore
      query_params[["source_ids"]] <- I(paste(lapply(`source_ids`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`lang` %in% c("EN", "ES", "PT", ""))) {
        stop("Invalid value for lang when calling NewsApi$NewsV1ArticleList. Must be [EN, ES, PT, ].")
      }
      query_params[["lang"]] <- `lang`

      # no explore
      # validate enum values
      for (query_item in `categories`) {
        if (!(query_item %in% c("1INCH", "AAVE", "ADA", "ALGO", "ALTCOIN", "ASIA", "ATOM", "AVAX", "AXS", "BAL", "BAT", "BCH", "BLOCKCHAIN", "BTC", "BUSINESS", "COMMODITY", "COMP", "CRV", "DASH", "DOGE", "DOT", "ENJ", "ETC", "ETH", "EXCHANGE", "FIAT", "FIL", "FTM", "ICO", "KNC", "LINK", "LRC", "LTC", "LUNA", "LUNC", "MANA", "MARKET", "MATIC", "MINING", "MKR", "REGULATION", "REN", "SAND", "SHIB", "SNX", "SOL", "SPONSORED", "SUSHI", "TECHNOLOGY", "TRADING", "TRX", "UMA", "UNI", "USDT", "WALLET", "XLM", "XMR", "XRP", "XTZ", "YFI", "ZEC", "ZRX", ""))) {
          stop("Invalid value for categories when calling NewsApi$NewsV1ArticleList. Must be [1INCH, AAVE, ADA, ALGO, ALTCOIN, ASIA, ATOM, AVAX, AXS, BAL, BAT, BCH, BLOCKCHAIN, BTC, BUSINESS, COMMODITY, COMP, CRV, DASH, DOGE, DOT, ENJ, ETC, ETH, EXCHANGE, FIAT, FIL, FTM, ICO, KNC, LINK, LRC, LTC, LUNA, LUNC, MANA, MARKET, MATIC, MINING, MKR, REGULATION, REN, SAND, SHIB, SNX, SOL, SPONSORED, SUSHI, TECHNOLOGY, TRADING, TRX, UMA, UNI, USDT, WALLET, XLM, XMR, XRP, XTZ, YFI, ZEC, ZRX, ].")
        }
      }
      query_params[["categories"]] <- I(paste(lapply(`categories`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      # validate enum values
      for (query_item in `exclude_categories`) {
        if (!(query_item %in% c("1INCH", "AAVE", "ADA", "ALGO", "ALTCOIN", "ASIA", "ATOM", "AVAX", "AXS", "BAL", "BAT", "BCH", "BLOCKCHAIN", "BTC", "BUSINESS", "COMMODITY", "COMP", "CRV", "DASH", "DOGE", "DOT", "ENJ", "ETC", "ETH", "EXCHANGE", "FIAT", "FIL", "FTM", "ICO", "KNC", "LINK", "LRC", "LTC", "LUNA", "LUNC", "MANA", "MARKET", "MATIC", "MINING", "MKR", "REGULATION", "REN", "SAND", "SHIB", "SNX", "SOL", "SPONSORED", "SUSHI", "TECHNOLOGY", "TRADING", "TRX", "UMA", "UNI", "USDT", "WALLET", "XLM", "XMR", "XRP", "XTZ", "YFI", "ZEC", "ZRX", ""))) {
          stop("Invalid value for exclude_categories when calling NewsApi$NewsV1ArticleList. Must be [1INCH, AAVE, ADA, ALGO, ALTCOIN, ASIA, ATOM, AVAX, AXS, BAL, BAT, BCH, BLOCKCHAIN, BTC, BUSINESS, COMMODITY, COMP, CRV, DASH, DOGE, DOT, ENJ, ETC, ETH, EXCHANGE, FIAT, FIL, FTM, ICO, KNC, LINK, LRC, LTC, LUNA, LUNC, MANA, MARKET, MATIC, MINING, MKR, REGULATION, REN, SAND, SHIB, SNX, SOL, SPONSORED, SUSHI, TECHNOLOGY, TRADING, TRX, UMA, UNI, USDT, WALLET, XLM, XMR, XRP, XTZ, YFI, ZEC, ZRX, ].")
        }
      }
      query_params[["exclude_categories"]] <- I(paste(lapply(`exclude_categories`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["to_ts"]] <- `to_ts`

      local_var_url_path <- "/news/v1/article/list"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    NewsV1CategoryList = function(data_file = NULL, ...) {
      local_var_response <- self$NewsV1CategoryListWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    NewsV1CategoryListWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/news/v1/category/list"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param lang (optional) The article source Preferred language - English (EN), Portuguese (PT), Espanol (ES) (default value: "EN")
    #' @param type (optional) RSS, API, TWITTER (default value: "RSS")
    #' @param status (optional) The status for the  article source entry. Allowed values: ACTIVE, INACTIVE (default value: "ACTIVE")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    NewsV1SourceList = function(lang = "EN", type = "RSS", status = "ACTIVE", data_file = NULL, ...) {
      local_var_response <- self$NewsV1SourceListWithHttpInfo(lang, type, status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param lang (optional) The article source Preferred language - English (EN), Portuguese (PT), Espanol (ES) (default value: "EN")
    #' @param type (optional) RSS, API, TWITTER (default value: "RSS")
    #' @param status (optional) The status for the  article source entry. Allowed values: ACTIVE, INACTIVE (default value: "ACTIVE")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    NewsV1SourceListWithHttpInfo = function(lang = "EN", type = "RSS", status = "ACTIVE", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`lang`) < 1) {
        stop("Invalid length for `lang` when calling NewsApi$NewsV1SourceList, must be bigger than or equal to 1.")
      }

      if (nchar(`type`) < 1) {
        stop("Invalid length for `type` when calling NewsApi$NewsV1SourceList, must be bigger than or equal to 1.")
      }

      if (nchar(`status`) < 1) {
        stop("Invalid length for `status` when calling NewsApi$NewsV1SourceList, must be bigger than or equal to 1.")
      }

      if (!(`lang` %in% c("EN", "ES", "PT", ""))) {
        stop("Invalid value for lang when calling NewsApi$NewsV1SourceList. Must be [EN, ES, PT, ].")
      }
      query_params[["lang"]] <- `lang`

      if (!(`type` %in% c("RSS", "API", "TWITTER", ""))) {
        stop("Invalid value for type when calling NewsApi$NewsV1SourceList. Must be [RSS, API, TWITTER, ].")
      }
      query_params[["type"]] <- `type`

      if (!(`status` %in% c("ACTIVE", "INACTIVE", ""))) {
        stop("Invalid value for status when calling NewsApi$NewsV1SourceList. Must be [ACTIVE, INACTIVE, ].")
      }
      query_params[["status"]] <- `status`

      local_var_url_path <- "/news/v1/source/list"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
