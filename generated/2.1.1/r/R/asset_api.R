#' CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
#'
#' CryptoCompare is the world's leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
#'
#' The version of the OpenAPI document: 2.1.1
#' Contact: data@cryptocompare.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Asset operations
#' @description AssetApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ AssetV1DataByAddress } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } address character
#' \item \emph{ @param } chain_symbol character
#' \item \emph{ @param } groups character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ AssetV1DataById } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } asset_id integer
#' \item \emph{ @param } groups character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ AssetV1DataBySymbol } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } asset_symbol character
#' \item \emph{ @param } groups character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ AssetV1TopList } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } page integer
#' \item \emph{ @param } page_size integer
#' \item \emph{ @param } asset_type character
#' \item \emph{ @param } sort_by Enum < [CREATED_ON, UPDATED_ON, SYMBOL] >
#' \item \emph{ @param } sort_direction Enum < [DESC, ASC] >
#' \item \emph{ @param } groups character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  AssetV1DataByAddress  ####################
#'
#' library(openapi)
#' var_address <- "address_example" # character | 
#' var_chain_symbol <- "chain_symbol_example" # character | 
#' var_groups <- "" # character |  (Optional)
#'
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetV1DataByAddress(var_address, var_chain_symbol, groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$AssetV1DataByAddress(var_address, var_chain_symbol, groups = var_groups)
#' dput(result)
#'
#'
#' ####################  AssetV1DataById  ####################
#'
#' library(openapi)
#' var_asset_id <- 56 # integer | The asset id you are interested in
#' var_groups <- "" # character |  (Optional)
#'
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetV1DataById(var_asset_id, groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$AssetV1DataById(var_asset_id, groups = var_groups)
#' dput(result)
#'
#'
#' ####################  AssetV1DataBySymbol  ####################
#'
#' library(openapi)
#' var_asset_symbol <- "asset_symbol_example" # character | The asset symbol you are interested in
#' var_groups <- "" # character |  (Optional)
#'
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetV1DataBySymbol(var_asset_symbol, groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$AssetV1DataBySymbol(var_asset_symbol, groups = var_groups)
#' dput(result)
#'
#'
#' ####################  AssetV1TopList  ####################
#'
#' library(openapi)
#' var_page <- 1 # integer | The page number for the request to get {page_size} coins at the time. (Optional)
#' var_page_size <- 100 # integer | The number of items returned per page (Optional)
#' var_asset_type <- "" # character | The asset class/type (Optional)
#' var_sort_by <- "CREATED_ON" # character | Sort by field ( CREATED_ON,UPDATED_ON,SYMBOL ) (Optional)
#' var_sort_direction <- "DESC" # character | Sort direction ( DESC,ASC ) (Optional)
#' var_groups <- "" # character |  (Optional)
#'
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetV1TopList(page = var_page, page_size = var_page_size, asset_type = var_asset_type, sort_by = var_sort_by, sort_direction = var_sort_direction, groups = var_groupsdata_file = "result.txt")
#' result <- api_instance$AssetV1TopList(page = var_page, page_size = var_page_size, asset_type = var_asset_type, sort_by = var_sort_by, sort_direction = var_sort_direction, groups = var_groups)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AssetApi <- R6::R6Class(
  "AssetApi",
  public = list(
    api_client = NULL,
    #' Initialize a new AssetApi.
    #'
    #' @description
    #' Initialize a new AssetApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param address 
    #' @param chain_symbol 
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    AssetV1DataByAddress = function(address, chain_symbol, groups = "", data_file = NULL, ...) {
      local_var_response <- self$AssetV1DataByAddressWithHttpInfo(address, chain_symbol, groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param address 
    #' @param chain_symbol 
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    AssetV1DataByAddressWithHttpInfo = function(address, chain_symbol, groups = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`address`)) {
        stop("Missing required parameter `address`.")
      }

      if (missing(`chain_symbol`)) {
        stop("Missing required parameter `chain_symbol`.")
      }

      if (nchar(`address`) > 500) {
        stop("Invalid length for `address` when calling AssetApi$AssetV1DataByAddress, must be smaller than or equal to 500.")
      }
      if (nchar(`address`) < 1) {
        stop("Invalid length for `address` when calling AssetApi$AssetV1DataByAddress, must be bigger than or equal to 1.")
      }

      if (nchar(`chain_symbol`) > 100) {
        stop("Invalid length for `chain_symbol` when calling AssetApi$AssetV1DataByAddress, must be smaller than or equal to 100.")
      }
      if (nchar(`chain_symbol`) < 1) {
        stop("Invalid length for `chain_symbol` when calling AssetApi$AssetV1DataByAddress, must be bigger than or equal to 1.")
      }

      if (nchar(`groups`) > 2000) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataByAddress, must be smaller than or equal to 2000.")
      }
      if (nchar(`groups`) < 0) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataByAddress, must be bigger than or equal to 0.")
      }

      query_params[["address"]] <- `address`

      query_params[["chain_symbol"]] <- `chain_symbol`

      query_params[["groups"]] <- `groups`

      local_var_url_path <- "/asset/v1/data/by/address"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param asset_id The asset id you are interested in
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    AssetV1DataById = function(asset_id, groups = "", data_file = NULL, ...) {
      local_var_response <- self$AssetV1DataByIdWithHttpInfo(asset_id, groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param asset_id The asset id you are interested in
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    AssetV1DataByIdWithHttpInfo = function(asset_id, groups = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }


      if (nchar(`groups`) > 2000) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataById, must be smaller than or equal to 2000.")
      }
      if (nchar(`groups`) < 0) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataById, must be bigger than or equal to 0.")
      }

      query_params[["asset_id"]] <- `asset_id`

      query_params[["groups"]] <- `groups`

      local_var_url_path <- "/asset/v1/data/by/id"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param asset_symbol The asset symbol you are interested in
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    AssetV1DataBySymbol = function(asset_symbol, groups = "", data_file = NULL, ...) {
      local_var_response <- self$AssetV1DataBySymbolWithHttpInfo(asset_symbol, groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param asset_symbol The asset symbol you are interested in
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    AssetV1DataBySymbolWithHttpInfo = function(asset_symbol, groups = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_symbol`)) {
        stop("Missing required parameter `asset_symbol`.")
      }

      if (nchar(`asset_symbol`) > 100) {
        stop("Invalid length for `asset_symbol` when calling AssetApi$AssetV1DataBySymbol, must be smaller than or equal to 100.")
      }
      if (nchar(`asset_symbol`) < 1) {
        stop("Invalid length for `asset_symbol` when calling AssetApi$AssetV1DataBySymbol, must be bigger than or equal to 1.")
      }

      if (nchar(`groups`) > 2000) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataBySymbol, must be smaller than or equal to 2000.")
      }
      if (nchar(`groups`) < 0) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1DataBySymbol, must be bigger than or equal to 0.")
      }

      query_params[["asset_symbol"]] <- `asset_symbol`

      query_params[["groups"]] <- `groups`

      local_var_url_path <- "/asset/v1/data/by/symbol"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param page (optional) The page number for the request to get {page_size} coins at the time. (default value: 1)
    #' @param page_size (optional) The number of items returned per page (default value: 100)
    #' @param asset_type (optional) The asset class/type (default value: "")
    #' @param sort_by (optional) Sort by field ( CREATED_ON,UPDATED_ON,SYMBOL ) (default value: "CREATED_ON")
    #' @param sort_direction (optional) Sort direction ( DESC,ASC ) (default value: "DESC")
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    AssetV1TopList = function(page = 1, page_size = 100, asset_type = "", sort_by = "CREATED_ON", sort_direction = "DESC", groups = "", data_file = NULL, ...) {
      local_var_response <- self$AssetV1TopListWithHttpInfo(page, page_size, asset_type, sort_by, sort_direction, groups, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param page (optional) The page number for the request to get {page_size} coins at the time. (default value: 1)
    #' @param page_size (optional) The number of items returned per page (default value: 100)
    #' @param asset_type (optional) The asset class/type (default value: "")
    #' @param sort_by (optional) Sort by field ( CREATED_ON,UPDATED_ON,SYMBOL ) (default value: "CREATED_ON")
    #' @param sort_direction (optional) Sort direction ( DESC,ASC ) (default value: "DESC")
    #' @param groups (optional) No description (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    AssetV1TopListWithHttpInfo = function(page = 1, page_size = 100, asset_type = "", sort_by = "CREATED_ON", sort_direction = "DESC", groups = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (`page` > 100) {
        stop("Invalid value for `page` when calling AssetApi$AssetV1TopList, must be smaller than or equal to 100.")
      }
      if (`page` < 1) {
        stop("Invalid value for `page` when calling AssetApi$AssetV1TopList, must be bigger than or equal to 1.")
      }

      if (`page_size` > 100) {
        stop("Invalid value for `page_size` when calling AssetApi$AssetV1TopList, must be smaller than or equal to 100.")
      }
      if (`page_size` < 10) {
        stop("Invalid value for `page_size` when calling AssetApi$AssetV1TopList, must be bigger than or equal to 10.")
      }




      if (nchar(`groups`) > 2000) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1TopList, must be smaller than or equal to 2000.")
      }
      if (nchar(`groups`) < 0) {
        stop("Invalid length for `groups` when calling AssetApi$AssetV1TopList, must be bigger than or equal to 0.")
      }

      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      query_params[["asset_type"]] <- `asset_type`

      if (!(`sort_by` %in% c("CREATED_ON", "UPDATED_ON", "SYMBOL"))) {
        stop("Invalid value for sort_by when calling AssetApi$AssetV1TopList. Must be [CREATED_ON, UPDATED_ON, SYMBOL].")
      }
      query_params[["sort_by"]] <- `sort_by`

      if (!(`sort_direction` %in% c("DESC", "ASC"))) {
        stop("Invalid value for sort_direction when calling AssetApi$AssetV1TopList. Must be [DESC, ASC].")
      }
      query_params[["sort_direction"]] <- `sort_direction`

      query_params[["groups"]] <- `groups`

      local_var_url_path <- "/asset/v1/top/list"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
