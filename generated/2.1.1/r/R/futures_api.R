#' CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
#'
#' CryptoCompare is the world's leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
#'
#' The version of the OpenAPI document: 2.1.1
#' Contact: data@cryptocompare.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Futures operations
#' @description FuturesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ FuturesV1HistoricalDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalFundingRateDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalFundingRateHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalFundingRateMessages } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } after_ts integer
#' \item \emph{ @param } last_ccseq integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalFundingRateMessagesHour } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } hour_ts integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalFundingRateMinutes } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalMinutes } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalOpenInterestDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalOpenInterestHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalOpenInterestMessages } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } after_ts integer
#' \item \emph{ @param } last_ccseq integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalOpenInterestMessagesHour } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } hour_ts integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalOpenInterestMinutes } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalTrades } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } after_ts integer
#' \item \emph{ @param } last_ccseq integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1HistoricalTradesHour } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } hour_ts integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1LatestFundingRateTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1LatestInstrumentMetadata } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1LatestOpenInterestTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1LatestTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1Markets } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1MarketsInstruments } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } instrument_status Enum < [ACTIVE, IGNORED, RETIRED, EXPIRED] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ FuturesV1MarketsInstrumentsUnmapped } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } instrument_status Enum < [ACTIVE, IGNORED, RETIRED, EXPIRED] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1HistoricalDays } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1HistoricalHours } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1HistoricalMessages } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } after_ts integer
#' \item \emph{ @param } last_ccseq integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1HistoricalMessagesHour } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } hour_ts integer
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @param } return_404_on_empty_response character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1HistoricalMinutes } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } to_ts integer
#' \item \emph{ @param } aggregate integer
#' \item \emph{ @param } fill character
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @param } response_format Enum < [JSON, CSV] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1LatestInstrumentMetadata } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1LatestTick } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instruments list( character )
#' \item \emph{ @param } groups list( character )
#' \item \emph{ @param } mapping_priority Enum < [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1Markets } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1MarketsInstruments } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } instrument_status Enum < [ACTIVE, IGNORED, RETIRED, EXPIRED] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IndexV1MarketsInstrumentsUnmapped } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } market character
#' \item \emph{ @param } instrument character
#' \item \emph{ @param } instrument_status Enum < [ACTIVE, IGNORED, RETIRED, EXPIRED] >
#' \item \emph{ @returnType } \link{GENERICRESPONSE} \cr
#'
#'
#' \item status code : 200 | Success response from the API.
#'
#' \item return type : GENERICRESPONSE
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The 400 error occurs when some of the data sent is malformed.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | The 401 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | The 403 error occurs when you don't use a valid API Key on an endpoint that requires authetication.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The 404 error can either be returned when some/all of parameters sent are not found within our system. This could be beacuse parameters like market, instrument, news source, symbol, asset_id etc. are invalid
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 405 | The 405 error occurs the user tries to use a http method (GET,POST,PUT etc) that is not supported.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | The 429 error occurs when you go over the API Key limit. Rate limits are eforced on a second (resets every second), minute (resers every minute), hour (resets every hour), day (resets every day) and month (resets every month) granularity. You can upgrade your account and access higher rate limits.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The 500 error occurs our API is up but does not know how to / can't handle the request.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 502 | The 502 error occurs when our API is not running. This error is returned by our proxy / load balancer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 503 | The 503 error occurs when there is an issue with one of our data sources and we can't even return a partial answer.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  FuturesV1HistoricalDays  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalFundingRateDays  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalFundingRateDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalFundingRateDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalFundingRateHours  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalFundingRateHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalFundingRateHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalFundingRateMessages  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_after_ts <- 56 # integer | Unix timestamp in seconds of the earliest funding rate message in the response (Optional)
#' var_last_ccseq <- 0 # integer | The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (Optional)
#' var_limit <- 100 # integer | The maximum number of funding rate messages to return (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalFundingRateMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalFundingRateMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalFundingRateMessagesHour  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_hour_ts <- 56 # integer | Unix timestamp in seconds for the hour containing the funding rate updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all funding rate updates in that hour. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalFundingRateMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalFundingRateMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalFundingRateMinutes  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalFundingRateMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalFundingRateMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalHours  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalMinutes  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalOpenInterestDays  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalOpenInterestDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalOpenInterestDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalOpenInterestHours  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalOpenInterestHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalOpenInterestHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalOpenInterestMessages  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_after_ts <- 56 # integer | Unix timestamp in seconds of the earliest open interest message in the response (Optional)
#' var_last_ccseq <- 0 # integer | The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (Optional)
#' var_limit <- 100 # integer | The maximum number of open interest messages to return (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalOpenInterestMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalOpenInterestMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalOpenInterestMessagesHour  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_hour_ts <- 56 # integer | Unix timestamp in seconds for the hour containing the open interest updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all open interest updates in that hour. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalOpenInterestMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalOpenInterestMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalOpenInterestMinutes  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalOpenInterestMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalOpenInterestMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalTrades  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_after_ts <- 56 # integer | Unix timestamp in seconds of the earliest trade in the response. (Optional)
#' var_last_ccseq <- 0 # integer | The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (Optional)
#' var_limit <- 100 # integer | The maximum number of trades to return (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalTrades(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalTrades(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1HistoricalTradesHour  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_hour_ts <- 56 # integer | Unix timestamp in seconds for the hour containing the trades you are interested in. You can pass any timestamp within an hour but we will round it down the the full hour timestmap and return all trades in that hour. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1HistoricalTradesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$FuturesV1HistoricalTradesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  FuturesV1LatestFundingRateTick  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1LatestFundingRateTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$FuturesV1LatestFundingRateTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  FuturesV1LatestInstrumentMetadata  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1LatestInstrumentMetadata(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$FuturesV1LatestInstrumentMetadata(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  FuturesV1LatestOpenInterestTick  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1LatestOpenInterestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$FuturesV1LatestOpenInterestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  FuturesV1LatestTick  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,TOP_OF_BOOK,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1LatestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$FuturesV1LatestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  FuturesV1Markets  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1Markets(market = var_marketdata_file = "result.txt")
#' result <- api_instance$FuturesV1Markets(market = var_market)
#' dput(result)
#'
#'
#' ####################  FuturesV1MarketsInstruments  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#' var_instrument <- "" # character | The mapped instrument to retrieve on a specific market. (Optional)
#' var_instrument_status <- c("ACTIVE") # array[character] | The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1MarketsInstruments(market = var_market, instrument = var_instrument, instrument_status = var_instrument_statusdata_file = "result.txt")
#' result <- api_instance$FuturesV1MarketsInstruments(market = var_market, instrument = var_instrument, instrument_status = var_instrument_status)
#' dput(result)
#'
#'
#' ####################  FuturesV1MarketsInstrumentsUnmapped  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#' var_instrument <- "" # character | The unmapped instrument to retrieve on a specific market. (Optional)
#' var_instrument_status <- c("ACTIVE") # array[character] | The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FuturesV1MarketsInstrumentsUnmapped(market = var_market, instrument = var_instrument, instrument_status = var_instrument_statusdata_file = "result.txt")
#' result <- api_instance$FuturesV1MarketsInstrumentsUnmapped(market = var_market, instrument = var_instrument, instrument_status = var_instrument_status)
#' dput(result)
#'
#'
#' ####################  IndexV1HistoricalDays  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1HistoricalDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$IndexV1HistoricalDays(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  IndexV1HistoricalHours  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1HistoricalHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$IndexV1HistoricalHours(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  IndexV1HistoricalMessages  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_after_ts <- 56 # integer | Unix timestamp in seconds of the earliest index message in the response (Optional)
#' var_last_ccseq <- 0 # integer | The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (Optional)
#' var_limit <- 100 # integer | The maximum number of index messages to return (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1HistoricalMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$IndexV1HistoricalMessages(var_market, var_instrument, after_ts = var_after_ts, last_ccseq = var_last_ccseq, limit = var_limit, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  IndexV1HistoricalMessagesHour  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_hour_ts <- 56 # integer | Unix timestamp in seconds for the hour containing the index updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all index updates in that hour. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#' var_return_404_on_empty_response <- FALSE # character | If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # Configure API key authorization: api_key_header
#' api_instance$api_client$api_keys["authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: api_key_query
#' api_instance$api_client$api_keys["api_key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1HistoricalMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_responsedata_file = "result.txt")
#' result <- api_instance$IndexV1HistoricalMessagesHour(var_market, var_instrument, hour_ts = var_hour_ts, mapping_priority = var_mapping_priority, response_format = var_response_format, return_404_on_empty_response = var_return_404_on_empty_response)
#' dput(result)
#'
#'
#' ####################  IndexV1HistoricalMinutes  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instrument <- "instrument_example" # character | The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (Optional)
#' var_limit <- 30 # integer | The number of data points to return (Optional)
#' var_to_ts <- 56 # integer | Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received} (Optional)
#' var_aggregate <- 1 # integer | The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (Optional)
#' var_fill <- TRUE # character | Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#' var_response_format <- "JSON" # character | The format of the data response in uppercase. It can be one of the following: JSON,CSV (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1HistoricalMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$IndexV1HistoricalMinutes(var_market, var_instrument, groups = var_groups, limit = var_limit, to_ts = var_to_ts, aggregate = var_aggregate, fill = var_fill, mapping_priority = var_mapping_priority, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  IndexV1LatestInstrumentMetadata  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1LatestInstrumentMetadata(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$IndexV1LatestInstrumentMetadata(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  IndexV1LatestTick  ####################
#'
#' library(openapi)
#' var_market <- "market_example" # character | The exchange to obtain data from
#' var_instruments <- c("inner_example") # array[character] | A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
#' var_groups <- c("inner_example") # array[character] | When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (Optional)
#' var_mapping_priority <- "CHECK_MAPPED_FIRST" # character | The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1LatestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_prioritydata_file = "result.txt")
#' result <- api_instance$IndexV1LatestTick(var_market, var_instruments, groups = var_groups, mapping_priority = var_mapping_priority)
#' dput(result)
#'
#'
#' ####################  IndexV1Markets  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1Markets(market = var_marketdata_file = "result.txt")
#' result <- api_instance$IndexV1Markets(market = var_market)
#' dput(result)
#'
#'
#' ####################  IndexV1MarketsInstruments  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#' var_instrument <- "" # character | The mapped instrument to retrieve on a specific market. (Optional)
#' var_instrument_status <- c("ACTIVE") # array[character] | The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1MarketsInstruments(market = var_market, instrument = var_instrument, instrument_status = var_instrument_statusdata_file = "result.txt")
#' result <- api_instance$IndexV1MarketsInstruments(market = var_market, instrument = var_instrument, instrument_status = var_instrument_status)
#' dput(result)
#'
#'
#' ####################  IndexV1MarketsInstrumentsUnmapped  ####################
#'
#' library(openapi)
#' var_market <- "" # character | The exchange to obtain data from (Optional)
#' var_instrument <- "" # character | The unmapped instrument to retrieve on a specific market. (Optional)
#' var_instrument_status <- c("ACTIVE") # array[character] | The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (Optional)
#'
#' api_instance <- FuturesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexV1MarketsInstrumentsUnmapped(market = var_market, instrument = var_instrument, instrument_status = var_instrument_statusdata_file = "result.txt")
#' result <- api_instance$IndexV1MarketsInstrumentsUnmapped(market = var_market, instrument = var_instrument, instrument_status = var_instrument_status)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FuturesApi <- R6::R6Class(
  "FuturesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new FuturesApi.
    #'
    #' @description
    #' Initialize a new FuturesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalDays = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalDaysWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalDaysWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalDays, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalDays, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalDays, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalDays, must be bigger than or equal to 1.")
      }


      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalDays, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalDays, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalDays. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalFundingRateDays = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalFundingRateDaysWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalFundingRateDaysWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be bigger than or equal to 1.")
      }


      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateDays, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalFundingRateDays. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalFundingRateDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/funding-rate/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalFundingRateHours = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalFundingRateHoursWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalFundingRateHoursWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateHours, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalFundingRateHours. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalFundingRateHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/funding-rate/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest funding rate message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of funding rate messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalFundingRateMessages = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalFundingRateMessagesWithHttpInfo(market, instrument, after_ts, last_ccseq, limit, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest funding rate message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of funding rate messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalFundingRateMessagesWithHttpInfo = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be bigger than or equal to 1.")
      }

      if (`after_ts` < 0) {
        stop("Invalid value for `after_ts` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be bigger than or equal to 0.")
      }

      if (`last_ccseq` < 0) {
        stop("Invalid value for `last_ccseq` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be bigger than or equal to 0.")
      }

      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateMessages, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["after_ts"]] <- `after_ts`

      query_params[["last_ccseq"]] <- `last_ccseq`

      query_params[["limit"]] <- `limit`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalFundingRateMessages. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalFundingRateMessages. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/funding-rate-messages"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the funding rate updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all funding rate updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalFundingRateMessagesHour = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalFundingRateMessagesHourWithHttpInfo(market, instrument, hour_ts, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the funding rate updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all funding rate updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalFundingRateMessagesHourWithHttpInfo = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour, must be bigger than or equal to 1.")
      }

      if (`hour_ts` < 0) {
        stop("Invalid value for `hour_ts` when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour, must be bigger than or equal to 0.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["hour_ts"]] <- `hour_ts`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalFundingRateMessagesHour. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/funding-rate-messages/hour"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalFundingRateMinutes = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalFundingRateMinutesWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalFundingRateMinutesWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalFundingRateMinutes. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/funding-rate/minutes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalHours = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalHoursWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalHoursWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalHours, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalHours, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalHours, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalHours, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalHours, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalHours. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalMinutes = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalMinutesWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalMinutesWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalMinutes, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalMinutes, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalMinutes, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalMinutes, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalMinutes, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalMinutes, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalMinutes, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalMinutes, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalMinutes. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalMinutes. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/minutes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalOpenInterestDays = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalOpenInterestDaysWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalOpenInterestDaysWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be bigger than or equal to 1.")
      }


      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestDays, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalOpenInterestDays. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalOpenInterestDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/open-interest/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalOpenInterestHours = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalOpenInterestHoursWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalOpenInterestHoursWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestHours, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalOpenInterestHours. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalOpenInterestHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/open-interest/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest open interest message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of open interest messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalOpenInterestMessages = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalOpenInterestMessagesWithHttpInfo(market, instrument, after_ts, last_ccseq, limit, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest open interest message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of open interest messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalOpenInterestMessagesWithHttpInfo = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be bigger than or equal to 1.")
      }

      if (`after_ts` < 0) {
        stop("Invalid value for `after_ts` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be bigger than or equal to 0.")
      }

      if (`last_ccseq` < 0) {
        stop("Invalid value for `last_ccseq` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be bigger than or equal to 0.")
      }

      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["after_ts"]] <- `after_ts`

      query_params[["last_ccseq"]] <- `last_ccseq`

      query_params[["limit"]] <- `limit`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalOpenInterestMessages. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/open-interest-messages"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the open interest updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all open interest updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalOpenInterestMessagesHour = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalOpenInterestMessagesHourWithHttpInfo(market, instrument, hour_ts, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the open interest updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all open interest updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalOpenInterestMessagesHourWithHttpInfo = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour, must be bigger than or equal to 1.")
      }

      if (`hour_ts` < 0) {
        stop("Invalid value for `hour_ts` when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour, must be bigger than or equal to 0.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["hour_ts"]] <- `hour_ts`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalOpenInterestMessagesHour. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/open-interest-messages/hour"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalOpenInterestMinutes = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalOpenInterestMinutesWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalOpenInterestMinutesWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalOpenInterestMinutes. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/futures/v1/historical/open-interest/minutes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest trade in the response.
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of trades to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalTrades = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalTradesWithHttpInfo(market, instrument, after_ts, last_ccseq, limit, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest trade in the response.
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of trades to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalTradesWithHttpInfo = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalTrades, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalTrades, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalTrades, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalTrades, must be bigger than or equal to 1.")
      }

      if (`after_ts` < 0) {
        stop("Invalid value for `after_ts` when calling FuturesApi$FuturesV1HistoricalTrades, must be bigger than or equal to 0.")
      }

      if (`last_ccseq` < 0) {
        stop("Invalid value for `last_ccseq` when calling FuturesApi$FuturesV1HistoricalTrades, must be bigger than or equal to 0.")
      }

      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalTrades, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$FuturesV1HistoricalTrades, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["after_ts"]] <- `after_ts`

      query_params[["last_ccseq"]] <- `last_ccseq`

      query_params[["limit"]] <- `limit`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalTrades. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalTrades. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/trades"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the trades you are interested in. You can pass any timestamp within an hour but we will round it down the the full hour timestmap and return all trades in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1HistoricalTradesHour = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$FuturesV1HistoricalTradesHourWithHttpInfo(market, instrument, hour_ts, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the trades you are interested in. You can pass any timestamp within an hour but we will round it down the the full hour timestmap and return all trades in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1HistoricalTradesHourWithHttpInfo = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalTradesHour, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1HistoricalTradesHour, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalTradesHour, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1HistoricalTradesHour, must be bigger than or equal to 1.")
      }

      if (`hour_ts` < 0) {
        stop("Invalid value for `hour_ts` when calling FuturesApi$FuturesV1HistoricalTradesHour, must be bigger than or equal to 0.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["hour_ts"]] <- `hour_ts`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1HistoricalTradesHour. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$FuturesV1HistoricalTradesHour. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/futures/v1/historical/trades/hour"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1LatestFundingRateTick = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1LatestFundingRateTickWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1LatestFundingRateTickWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestFundingRateTick, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestFundingRateTick, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestFundingRateTick, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestFundingRateTick, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1LatestFundingRateTick. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/futures/v1/latest/funding-rate/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1LatestInstrumentMetadata = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1LatestInstrumentMetadataWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1LatestInstrumentMetadataWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestInstrumentMetadata, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestInstrumentMetadata, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestInstrumentMetadata, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestInstrumentMetadata, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1LatestInstrumentMetadata. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/futures/v1/latest/instrument/metadata"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1LatestOpenInterestTick = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1LatestOpenInterestTickWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1LatestOpenInterestTickWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestOpenInterestTick, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestOpenInterestTick, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestOpenInterestTick, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestOpenInterestTick, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1LatestOpenInterestTick. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/futures/v1/latest/open-interest/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,TOP_OF_BOOK,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1LatestTick = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1LatestTickWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,TOP_OF_BOOK,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1LatestTickWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestTick, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1LatestTick, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestTick, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$FuturesV1LatestTick, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$FuturesV1LatestTick. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/futures/v1/latest/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1Markets = function(market = "", data_file = NULL, ...) {
      local_var_response <- self$FuturesV1MarketsWithHttpInfo(market, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1MarketsWithHttpInfo = function(market = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1Markets, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1Markets, must be bigger than or equal to 0.")
      }

      query_params[["market"]] <- `market`

      local_var_url_path <- "/futures/v1/markets"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The mapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1MarketsInstruments = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      local_var_response <- self$FuturesV1MarketsInstrumentsWithHttpInfo(market, instrument, instrument_status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The mapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1MarketsInstrumentsWithHttpInfo = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1MarketsInstruments, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1MarketsInstruments, must be bigger than or equal to 0.")
      }

      if (nchar(`instrument`) > 30) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1MarketsInstruments, must be smaller than or equal to 30.")
      }
      if (nchar(`instrument`) < 0) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1MarketsInstruments, must be bigger than or equal to 0.")
      }


      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      # validate enum values
      for (query_item in `instrument_status`) {
        if (!(query_item %in% c("ACTIVE", "IGNORED", "RETIRED", "EXPIRED"))) {
          stop("Invalid value for instrument_status when calling FuturesApi$FuturesV1MarketsInstruments. Must be [ACTIVE, IGNORED, RETIRED, EXPIRED].")
        }
      }
      query_params[["instrument_status"]] <- I(paste(lapply(`instrument_status`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/futures/v1/markets/instruments"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The unmapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    FuturesV1MarketsInstrumentsUnmapped = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      local_var_response <- self$FuturesV1MarketsInstrumentsUnmappedWithHttpInfo(market, instrument, instrument_status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The unmapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    FuturesV1MarketsInstrumentsUnmappedWithHttpInfo = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1MarketsInstrumentsUnmapped, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$FuturesV1MarketsInstrumentsUnmapped, must be bigger than or equal to 0.")
      }

      if (nchar(`instrument`) > 30) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1MarketsInstrumentsUnmapped, must be smaller than or equal to 30.")
      }
      if (nchar(`instrument`) < 0) {
        stop("Invalid length for `instrument` when calling FuturesApi$FuturesV1MarketsInstrumentsUnmapped, must be bigger than or equal to 0.")
      }


      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      # validate enum values
      for (query_item in `instrument_status`) {
        if (!(query_item %in% c("ACTIVE", "IGNORED", "RETIRED", "EXPIRED"))) {
          stop("Invalid value for instrument_status when calling FuturesApi$FuturesV1MarketsInstrumentsUnmapped. Must be [ACTIVE, IGNORED, RETIRED, EXPIRED].")
        }
      }
      query_params[["instrument_status"]] <- I(paste(lapply(`instrument_status`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/futures/v1/markets/instruments/unmapped"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1HistoricalDays = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$IndexV1HistoricalDaysWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1HistoricalDaysWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalDays, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalDays, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalDays, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalDays, must be bigger than or equal to 1.")
      }


      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalDays, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalDays, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalDays, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalDays, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1HistoricalDays. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$IndexV1HistoricalDays. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/index/v1/historical/days"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1HistoricalHours = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$IndexV1HistoricalHoursWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1HistoricalHoursWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalHours, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalHours, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalHours, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalHours, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalHours, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalHours, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalHours, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalHours, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1HistoricalHours. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$IndexV1HistoricalHours. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/index/v1/historical/hours"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest index message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of index messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1HistoricalMessages = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$IndexV1HistoricalMessagesWithHttpInfo(market, instrument, after_ts, last_ccseq, limit, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param after_ts (optional) Unix timestamp in seconds of the earliest index message in the response
    #' @param last_ccseq (optional) The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (default value: 0)
    #' @param limit (optional) The maximum number of index messages to return (default value: 100)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1HistoricalMessagesWithHttpInfo = function(market, instrument, after_ts = NULL, last_ccseq = 0, limit = 100, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMessages, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMessages, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMessages, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMessages, must be bigger than or equal to 1.")
      }

      if (`after_ts` < 0) {
        stop("Invalid value for `after_ts` when calling FuturesApi$IndexV1HistoricalMessages, must be bigger than or equal to 0.")
      }

      if (`last_ccseq` < 0) {
        stop("Invalid value for `last_ccseq` when calling FuturesApi$IndexV1HistoricalMessages, must be bigger than or equal to 0.")
      }

      if (`limit` > 5000) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalMessages, must be smaller than or equal to 5000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalMessages, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["after_ts"]] <- `after_ts`

      query_params[["last_ccseq"]] <- `last_ccseq`

      query_params[["limit"]] <- `limit`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1HistoricalMessages. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$IndexV1HistoricalMessages. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/index/v1/historical/messages"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the index updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all index updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1HistoricalMessagesHour = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      local_var_response <- self$IndexV1HistoricalMessagesHourWithHttpInfo(market, instrument, hour_ts, mapping_priority, response_format, return_404_on_empty_response, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param hour_ts (optional) Unix timestamp in seconds for the hour containing the index updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all index updates in that hour.
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param return_404_on_empty_response (optional) If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1HistoricalMessagesHourWithHttpInfo = function(market, instrument, hour_ts = NULL, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", return_404_on_empty_response = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMessagesHour, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMessagesHour, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMessagesHour, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMessagesHour, must be bigger than or equal to 1.")
      }

      if (`hour_ts` < 0) {
        stop("Invalid value for `hour_ts` when calling FuturesApi$IndexV1HistoricalMessagesHour, must be bigger than or equal to 0.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      query_params[["hour_ts"]] <- `hour_ts`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1HistoricalMessagesHour. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$IndexV1HistoricalMessagesHour. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      query_params[["return_404_on_empty_response"]] <- `return_404_on_empty_response`

      local_var_url_path <- "/index/v1/historical/messages/hour"
      # API key authentication
      if ("authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["authorization"]) > 0) {
        header_params["authorization"] <- paste(unlist(self$api_client$api_keys["authorization"]), collapse = "")
      }
      # API key authentication
      if ("api_key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["api_key"]) > 0) {
        query_params["api_key"] <- paste(unlist(self$api_client$api_keys["api_key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1HistoricalMinutes = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      local_var_response <- self$IndexV1HistoricalMinutesWithHttpInfo(market, instrument, groups, limit, to_ts, aggregate, fill, mapping_priority, response_format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (default value: [])
    #' @param limit (optional) The number of data points to return (default value: 30)
    #' @param to_ts (optional) Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit=2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &limit=2000&to_ts={the earliest unix timestamp received}
    #' @param aggregate (optional) The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (default value: 1)
    #' @param fill (optional) Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (default value: TRUE)
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param response_format (optional) The format of the data response in uppercase. It can be one of the following: JSON,CSV (default value: "JSON")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1HistoricalMinutesWithHttpInfo = function(market, instrument, groups = [], limit = 30, to_ts = NULL, aggregate = 1, fill = TRUE, mapping_priority = "CHECK_MAPPED_FIRST", response_format = "JSON", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instrument`)) {
        stop("Missing required parameter `instrument`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMinutes, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1HistoricalMinutes, must be bigger than or equal to 2.")
      }

      if (nchar(`instrument`) > 500) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMinutes, must be smaller than or equal to 500.")
      }
      if (nchar(`instrument`) < 1) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1HistoricalMinutes, must be bigger than or equal to 1.")
      }


      if (`limit` > 2000) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalMinutes, must be smaller than or equal to 2000.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling FuturesApi$IndexV1HistoricalMinutes, must be bigger than or equal to 1.")
      }


      if (`aggregate` > 30) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalMinutes, must be smaller than or equal to 30.")
      }
      if (`aggregate` < 1) {
        stop("Invalid value for `aggregate` when calling FuturesApi$IndexV1HistoricalMinutes, must be bigger than or equal to 1.")
      }




      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["limit"]] <- `limit`

      query_params[["to_ts"]] <- `to_ts`

      query_params[["aggregate"]] <- `aggregate`

      query_params[["fill"]] <- `fill`

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1HistoricalMinutes. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      if (!(`response_format` %in% c("JSON", "CSV"))) {
        stop("Invalid value for response_format when calling FuturesApi$IndexV1HistoricalMinutes. Must be [JSON, CSV].")
      }
      query_params[["response_format"]] <- `response_format`

      local_var_url_path <- "/index/v1/historical/minutes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1LatestInstrumentMetadata = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$IndexV1LatestInstrumentMetadataWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1LatestInstrumentMetadataWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1LatestInstrumentMetadata, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1LatestInstrumentMetadata, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$IndexV1LatestInstrumentMetadata, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$IndexV1LatestInstrumentMetadata, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1LatestInstrumentMetadata. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/index/v1/latest/instrument/metadata"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1LatestTick = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      local_var_response <- self$IndexV1LatestTickWithHttpInfo(market, instruments, groups, mapping_priority, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market The exchange to obtain data from
    #' @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first.
    #' @param groups (optional) When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (default value: [])
    #' @param mapping_priority (optional) The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (default value: "CHECK_MAPPED_FIRST")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1LatestTickWithHttpInfo = function(market, instruments, groups = [], mapping_priority = "CHECK_MAPPED_FIRST", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`market`)) {
        stop("Missing required parameter `market`.")
      }

      if (missing(`instruments`)) {
        stop("Missing required parameter `instruments`.")
      }

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1LatestTick, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 2) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1LatestTick, must be bigger than or equal to 2.")
      }

      if (length(`instruments`) > 25) {
        stop("Invalid length for `instruments` when calling FuturesApi$IndexV1LatestTick, number of items must be less than or equal to 25.")
      }
      if (length(`instruments`) < 1) {
        stop("Invalid length for `instruments` when calling FuturesApi$IndexV1LatestTick, number of items must be greater than or equal to 1.")
      }



      query_params[["market"]] <- `market`

      # no explore
      query_params[["instruments"]] <- I(paste(lapply(`instruments`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["groups"]] <- I(paste(lapply(`groups`, URLencode, reserved = TRUE), collapse = ","))

      if (!(`mapping_priority` %in% c("CHECK_MAPPED_FIRST", "CHECK_UNMAPPED_FIRST"))) {
        stop("Invalid value for mapping_priority when calling FuturesApi$IndexV1LatestTick. Must be [CHECK_MAPPED_FIRST, CHECK_UNMAPPED_FIRST].")
      }
      query_params[["mapping_priority"]] <- `mapping_priority`

      local_var_url_path <- "/index/v1/latest/tick"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1Markets = function(market = "", data_file = NULL, ...) {
      local_var_response <- self$IndexV1MarketsWithHttpInfo(market, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1MarketsWithHttpInfo = function(market = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1Markets, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1Markets, must be bigger than or equal to 0.")
      }

      query_params[["market"]] <- `market`

      local_var_url_path <- "/index/v1/markets"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The mapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1MarketsInstruments = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      local_var_response <- self$IndexV1MarketsInstrumentsWithHttpInfo(market, instrument, instrument_status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The mapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1MarketsInstrumentsWithHttpInfo = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1MarketsInstruments, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1MarketsInstruments, must be bigger than or equal to 0.")
      }

      if (nchar(`instrument`) > 30) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1MarketsInstruments, must be smaller than or equal to 30.")
      }
      if (nchar(`instrument`) < 0) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1MarketsInstruments, must be bigger than or equal to 0.")
      }


      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      # validate enum values
      for (query_item in `instrument_status`) {
        if (!(query_item %in% c("ACTIVE", "IGNORED", "RETIRED", "EXPIRED"))) {
          stop("Invalid value for instrument_status when calling FuturesApi$IndexV1MarketsInstruments. Must be [ACTIVE, IGNORED, RETIRED, EXPIRED].")
        }
      }
      query_params[["instrument_status"]] <- I(paste(lapply(`instrument_status`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/index/v1/markets/instruments"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The unmapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GENERICRESPONSE
    #' @export
    IndexV1MarketsInstrumentsUnmapped = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      local_var_response <- self$IndexV1MarketsInstrumentsUnmappedWithHttpInfo(market, instrument, instrument_status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param market (optional) The exchange to obtain data from (default value: "")
    #' @param instrument (optional) The unmapped instrument to retrieve on a specific market. (default value: "")
    #' @param instrument_status (optional) The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GENERICRESPONSE) with additional information such as HTTP status code, headers
    #' @export
    IndexV1MarketsInstrumentsUnmappedWithHttpInfo = function(market = "", instrument = "", instrument_status = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`market`) > 30) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1MarketsInstrumentsUnmapped, must be smaller than or equal to 30.")
      }
      if (nchar(`market`) < 0) {
        stop("Invalid length for `market` when calling FuturesApi$IndexV1MarketsInstrumentsUnmapped, must be bigger than or equal to 0.")
      }

      if (nchar(`instrument`) > 30) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1MarketsInstrumentsUnmapped, must be smaller than or equal to 30.")
      }
      if (nchar(`instrument`) < 0) {
        stop("Invalid length for `instrument` when calling FuturesApi$IndexV1MarketsInstrumentsUnmapped, must be bigger than or equal to 0.")
      }


      query_params[["market"]] <- `market`

      query_params[["instrument"]] <- `instrument`

      # no explore
      # validate enum values
      for (query_item in `instrument_status`) {
        if (!(query_item %in% c("ACTIVE", "IGNORED", "RETIRED", "EXPIRED"))) {
          stop("Invalid value for instrument_status when calling FuturesApi$IndexV1MarketsInstrumentsUnmapped. Must be [ACTIVE, IGNORED, RETIRED, EXPIRED].")
        }
      }
      query_params[["instrument_status"]] <- I(paste(lapply(`instrument_status`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/index/v1/markets/instruments/unmapped"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GENERICRESPONSE", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
