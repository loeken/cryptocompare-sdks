/**
 * CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
 * CryptoCompare is the world\'s leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
 *
 * The version of the OpenAPI document: 2.1.1
 * Contact: data@cryptocompare.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  GENERICRESPONSE,
} from './models';

/**
 * overviewV1HistoricalMarketcapAllAssetsDays - parameters interface
 */
export interface IOverviewV1HistoricalMarketcapAllAssetsDaysParams {
  groups?: Array<string>;
  limit?: number;
  toTs?: number;
  aggregate?: number;
  fill?: boolean;
  responseFormat?: 'JSON' | 'CSV';
}

/**
 * overviewV1HistoricalMarketcapAllAssetsHours - parameters interface
 */
export interface IOverviewV1HistoricalMarketcapAllAssetsHoursParams {
  groups?: Array<string>;
  limit?: number;
  toTs?: number;
  aggregate?: number;
  fill?: boolean;
  responseFormat?: 'JSON' | 'CSV';
}

/**
 * overviewV1HistoricalMarketcapFtwAssetsDays - parameters interface
 */
export interface IOverviewV1HistoricalMarketcapFtwAssetsDaysParams {
  groups?: Array<string>;
  limit?: number;
  toTs?: number;
  aggregate?: number;
  fill?: boolean;
  responseFormat?: 'JSON' | 'CSV';
}

/**
 * overviewV1HistoricalMarketcapFtwAssetsHours - parameters interface
 */
export interface IOverviewV1HistoricalMarketcapFtwAssetsHoursParams {
  groups?: Array<string>;
  limit?: number;
  toTs?: number;
  aggregate?: number;
  fill?: boolean;
  responseFormat?: 'JSON' | 'CSV';
}

/**
 * overviewV1LatestMarketcapAllTick - parameters interface
 */
export interface IOverviewV1LatestMarketcapAllTickParams {
  groups?: Array<string>;
}

/**
 * overviewV1LatestMarketcapFtwTick - parameters interface
 */
export interface IOverviewV1LatestMarketcapFtwTickParams {
  groups?: Array<string>;
}

/**
 * OverviewApi - API class
 */
@autoinject()
export class OverviewApi extends Api {

  /**
   * Creates a new OverviewApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * @param params.groups When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME
   * @param params.limit The number of data points to return
   * @param params.toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received}
   * @param params.aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries
   * @param params.fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity.
   * @param params.responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV
   */
  async overviewV1HistoricalMarketcapAllAssetsDays(params: IOverviewV1HistoricalMarketcapAllAssetsDaysParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/historical/marketcap/all/assets/days`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
        'limit': params['limit'],
        'to_ts': params['toTs'],
        'aggregate': params['aggregate'],
        'fill': params['fill'],
        'response_format': params['responseFormat'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * @param params.groups When requesting all market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME
   * @param params.limit The number of data points to return
   * @param params.toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received}
   * @param params.aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries
   * @param params.fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity.
   * @param params.responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV
   */
  async overviewV1HistoricalMarketcapAllAssetsHours(params: IOverviewV1HistoricalMarketcapAllAssetsHoursParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/historical/marketcap/all/assets/hours`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
        'limit': params['limit'],
        'to_ts': params['toTs'],
        'aggregate': params['aggregate'],
        'fill': params['fill'],
        'response_format': params['responseFormat'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * @param params.groups When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME
   * @param params.limit The number of data points to return
   * @param params.toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received}
   * @param params.aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries
   * @param params.fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity.
   * @param params.responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV
   */
  async overviewV1HistoricalMarketcapFtwAssetsDays(params: IOverviewV1HistoricalMarketcapFtwAssetsDaysParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/historical/marketcap/ftw/assets/days`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
        'limit': params['limit'],
        'to_ts': params['toTs'],
        'aggregate': params['aggregate'],
        'fill': params['fill'],
        'response_format': params['responseFormat'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * @param params.groups When requesting ftw universe market cap historical data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,VOLUME
   * @param params.limit The number of data points to return
   * @param params.toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received}
   * @param params.aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries
   * @param params.fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity.
   * @param params.responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV
   */
  async overviewV1HistoricalMarketcapFtwAssetsHours(params: IOverviewV1HistoricalMarketcapFtwAssetsHoursParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/historical/marketcap/ftw/assets/hours`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
        'limit': params['limit'],
        'to_ts': params['toTs'],
        'aggregate': params['aggregate'],
        'fill': params['fill'],
        'response_format': params['responseFormat'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * @param params.groups When requesting all market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR
   */
  async overviewV1LatestMarketcapAllTick(params: IOverviewV1LatestMarketcapAllTickParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/latest/marketcap/all/tick`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * @param params.groups When requesting ftw universe market cap tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,MOVING_24_HOUR
   */
  async overviewV1LatestMarketcapFtwTick(params: IOverviewV1LatestMarketcapFtwTickParams): Promise<GENERICRESPONSE> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/overview/v1/latest/marketcap/ftw/tick`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'groups': params['groups'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

