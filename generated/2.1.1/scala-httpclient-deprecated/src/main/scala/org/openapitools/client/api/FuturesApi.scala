/**
 * CryptoCompare Market, Index, Reference Data, News and Overview Metrcis API for the Cryptocurrency Industry
 * CryptoCompare is the world's leading digital asset data company, providing services that cater to corporate, government and retail clients. Our main reference index is widely recognized as a reliable benchmark, and we have been authorized by the Financial Conduct Authority (FCA) in the United Kingdom to operate as a benchmark administrator. In addition to catering to corporate, government, and retail clients, we offer a range of services including comprehensive market data for cryptocurrency exchanges in both the centralized and decentralized finance sectors, as well as individual asset data on custodians, supported platforms, and privacy solutions. Our offerings include price, volume, trade, and orderbook updates, as well as historical OHLCV data.
 *
 * The version of the OpenAPI document: 2.1.1
 * Contact: data@cryptocompare.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package org.openapitools.client.api

import java.text.SimpleDateFormat

import org.openapitools.client.model.Error
import org.openapitools.client.model.GENERICRESPONSE
import org.openapitools.client.{ApiInvoker, ApiException}

import collection.mutable
import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart
import com.wordnik.swagger.client._
import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._
import javax.ws.rs.core.Response.Status.Family

import java.net.URI
import java.io.File
import java.util.Date
import java.util.TimeZone
import javax.ws.rs.core.{MediaType, Response}

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.collection.mutable.HashMap
import scala.util.{Failure, Success, Try}

import org.json4s._

class FuturesApi(
  val defBasePath: String = "https://data-api.cryptocompare.com",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new FuturesApiAsyncHelper(client, config)

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalDays(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalDaysAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalDaysAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalDays(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalFundingRateDays(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalFundingRateDaysAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalFundingRateDaysAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalFundingRateDays(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalFundingRateHours(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalFundingRateHoursAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalFundingRateHoursAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalFundingRateHours(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest funding rate message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of funding rate messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalFundingRateMessages(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalFundingRateMessagesAsync(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest funding rate message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of funding rate messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalFundingRateMessagesAsync(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalFundingRateMessages(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the funding rate updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all funding rate updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalFundingRateMessagesHour(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalFundingRateMessagesHourAsync(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the funding rate updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all funding rate updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalFundingRateMessagesHourAsync(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalFundingRateMessagesHour(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalFundingRateMinutes(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalFundingRateMinutesAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalFundingRateMinutesAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalFundingRateMinutes(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalHours(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalHoursAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalHoursAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalHours(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalMinutes(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalMinutesAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_TRADE,TRADE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalMinutesAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalMinutes(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalOpenInterestDays(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalOpenInterestDaysAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalOpenInterestDaysAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalOpenInterestDays(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalOpenInterestHours(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalOpenInterestHoursAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalOpenInterestHoursAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalOpenInterestHours(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest open interest message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of open interest messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalOpenInterestMessages(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalOpenInterestMessagesAsync(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest open interest message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of open interest messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalOpenInterestMessagesAsync(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalOpenInterestMessages(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the open interest updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all open interest updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalOpenInterestMessagesHour(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalOpenInterestMessagesHourAsync(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the open interest updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all open interest updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalOpenInterestMessagesHourAsync(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalOpenInterestMessagesHour(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalOpenInterestMinutes(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalOpenInterestMinutesAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,OHLC,OHLC_MESSAGE,MESSAGE (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalOpenInterestMinutesAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalOpenInterestMinutes(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest trade in the response. (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of trades to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalTrades(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalTradesAsync(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest trade in the response. (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of trades to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalTradesAsync(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalTrades(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the trades you are interested in. You can pass any timestamp within an hour but we will round it down the the full hour timestmap and return all trades in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def futuresV1HistoricalTradesHour(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1HistoricalTradesHourAsync(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the trades you are interested in. You can pass any timestamp within an hour but we will round it down the the full hour timestmap and return all trades in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1HistoricalTradesHourAsync(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1HistoricalTradesHour(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def futuresV1LatestFundingRateTick(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1LatestFundingRateTickAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1LatestFundingRateTickAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1LatestFundingRateTick(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def futuresV1LatestInstrumentMetadata(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1LatestInstrumentMetadataAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1LatestInstrumentMetadataAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1LatestInstrumentMetadata(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def futuresV1LatestOpenInterestTick(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1LatestOpenInterestTickAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1LatestOpenInterestTickAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1LatestOpenInterestTick(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,TOP_OF_BOOK,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def futuresV1LatestTick(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1LatestTickAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,MAPPING,VALUE,LAST_UPDATE,TOP_OF_BOOK,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1LatestTickAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1LatestTick(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @return GENERICRESPONSE
   */
  def futuresV1Markets(market: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1MarketsAsync(market), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1MarketsAsync(market: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1Markets(market)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The mapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return GENERICRESPONSE
   */
  def futuresV1MarketsInstruments(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1MarketsInstrumentsAsync(market, instrument, instrumentStatus), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The mapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1MarketsInstrumentsAsync(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1MarketsInstruments(market, instrument, instrumentStatus)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The unmapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return GENERICRESPONSE
   */
  def futuresV1MarketsInstrumentsUnmapped(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(futuresV1MarketsInstrumentsUnmappedAsync(market, instrument, instrumentStatus), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The unmapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return Future(GENERICRESPONSE)
   */
  def futuresV1MarketsInstrumentsUnmappedAsync(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Future[GENERICRESPONSE] = {
      helper.futuresV1MarketsInstrumentsUnmapped(market, instrument, instrumentStatus)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def indexV1HistoricalDays(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1HistoricalDaysAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1HistoricalDaysAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1HistoricalDays(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def indexV1HistoricalHours(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1HistoricalHoursAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1HistoricalHoursAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1HistoricalHours(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest index message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of index messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def indexV1HistoricalMessages(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1HistoricalMessagesAsync(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param afterTs Unix timestamp in seconds of the earliest index message in the response (optional)
   * @param lastCcseq The CCSEQ of the last message seen in the previous request. Useful for pagination within messages that happened in the same second. It will look though the messages in the same second and discard all messages until it reaches the CCSEQ from the last_ccseq parameter. If the CCSEQ is not part of that second, it will discard all messages in the second and only return messages starting from the next second onwards. When starting an integration with our API, on the first request, you should either not send a value for this paramater (defaults to 0 and does not discard any messages) or send 0. After you get a response use the TIMESTAMP and CCSEQ from the last message in the response. Pass the TIMESTAMP in the after_ts parameter and the CCSEQ in the last_ccseq parameter on your next request. (optional, default to 0)
   * @param limit The maximum number of index messages to return (optional, default to 100)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1HistoricalMessagesAsync(market: String, instrument: String, afterTs: Option[Integer] = None, lastCcseq: Option[Integer] = None, limit: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.indexV1HistoricalMessages(market, instrument, afterTs, lastCcseq, limit, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the index updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all index updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return GENERICRESPONSE
   */
  def indexV1HistoricalMessagesHour(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1HistoricalMessagesHourAsync(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param hourTs Unix timestamp in seconds for the hour containing the index updates you are interested in. You can pass any timestamp in the hour but we will round it down the the full hour timestmap and return all index updates in that hour. (optional)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @param return404OnEmptyResponse If set to false (default) then when there are no items to return, you will get a response status code of 200 and an empty JSON array or a CSV file with just the header. If set to true then when there are no items you will get a 404 status code for both JSON and CSV response_formats. (optional, default to false)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1HistoricalMessagesHourAsync(market: String, instrument: String, hourTs: Option[Integer] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None, return404OnEmptyResponse: Option[Boolean] = None): Future[GENERICRESPONSE] = {
      helper.indexV1HistoricalMessagesHour(market, instrument, hourTs, mappingPriority, responseFormat, return404OnEmptyResponse)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return GENERICRESPONSE
   */
  def indexV1HistoricalMinutes(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1HistoricalMinutesAsync(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instrument The mapped or unmapped instrument to retrieve on a specific market. We first check the instrument against the mapped list and then against the unmapped list          but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting historical entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,OHLC,OHLC_MESSAGE,MESSAGE,VOLUME (optional, default to [])
   * @param limit The number of data points to return (optional, default to 30)
   * @param toTs Returns historical data before this unix timestamp. If you want to get all the available historical data, you can use limit&#x3D;2000 and keep going back in time using the to_ts param. You can then keep requesting batches using: &amp;limit&#x3D;2000&amp;to_ts&#x3D;{the earliest unix timestamp received} (optional)
   * @param aggregate The number of points to aggregate for each returned value. E.g. passing 5 on a minute histo data endpoint will return data at 5 minute intervals. You are still limited to a maximum of 2000 minute points so the maximum you can get is 400 5 minutes interval entries (optional, default to 1)
   * @param fill Boolean value, if set to false or 0 we will not return data points for periods with no trading activity. (optional, default to true)
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @param responseFormat The format of the data response in uppercase. It can be one of the following: JSON,CSV (optional, default to JSON)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1HistoricalMinutesAsync(market: String, instrument: String, groups: Option[List[String]] = None, limit: Option[Integer] = None, toTs: Option[Integer] = None, aggregate: Option[Integer] = None, fill: Option[Boolean] = None, mappingPriority: Option[String] = None, responseFormat: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1HistoricalMinutes(market, instrument, groups, limit, toTs, aggregate, fill, mappingPriority, responseFormat)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def indexV1LatestInstrumentMetadata(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1LatestInstrumentMetadataAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting metadata entries you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: STATUS,INTERNAL,GENERAL,MIGRATION,SOURCE (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1LatestInstrumentMetadataAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1LatestInstrumentMetadata(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return GENERICRESPONSE
   */
  def indexV1LatestTick(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1LatestTickAsync(market, instruments, groups, mappingPriority), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from 
   * @param instruments A comma separated array of mapped and/or unmapped instruments to retrieve for a specific market (you can use either the instrument XXBTZUSD or mapped instrument (base - quote) BTC-USD on kraken as an example). We first check each instrument against the mapped list and then against the unmapped list but you can use the mapping_priority param to check the unmapped list first. 
   * @param groups When requesting tick data you can fiter by specific groups of interest. To do so just pass the groups of interest into the URL as a comma separted list. If left empty it will get all data that you account is allowed to access. The groups available are: ID,VALUE,LAST_UPDATE,CURRENT_HOUR,CURRENT_DAY,CURRENT_WEEK,CURRENT_MONTH,CURRENT_YEAR,MOVING_24_HOUR,MOVING_7_DAY,MOVING_30_DAY,MOVING_90_DAY,MOVING_180_DAY,MOVING_365_DAY,LIFETIME (optional, default to [])
   * @param mappingPriority The mapping priority of the instrument. It is set to CHECK_MAPPED_FIRST by default. It can be one of the following: CHECK_MAPPED_FIRST,CHECK_UNMAPPED_FIRST (optional, default to CHECK_MAPPED_FIRST)
   * @return Future(GENERICRESPONSE)
   */
  def indexV1LatestTickAsync(market: String, instruments: List[String], groups: Option[List[String]] = None, mappingPriority: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1LatestTick(market, instruments, groups, mappingPriority)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @return GENERICRESPONSE
   */
  def indexV1Markets(market: Option[String] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1MarketsAsync(market), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @return Future(GENERICRESPONSE)
   */
  def indexV1MarketsAsync(market: Option[String] = None): Future[GENERICRESPONSE] = {
      helper.indexV1Markets(market)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The mapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return GENERICRESPONSE
   */
  def indexV1MarketsInstruments(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1MarketsInstrumentsAsync(market, instrument, instrumentStatus), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The mapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return Future(GENERICRESPONSE)
   */
  def indexV1MarketsInstrumentsAsync(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Future[GENERICRESPONSE] = {
      helper.indexV1MarketsInstruments(market, instrument, instrumentStatus)
  }

  /**
   * 
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The unmapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return GENERICRESPONSE
   */
  def indexV1MarketsInstrumentsUnmapped(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Option[GENERICRESPONSE] = {
    val await = Try(Await.result(indexV1MarketsInstrumentsUnmappedAsync(market, instrument, instrumentStatus), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * 
   *
   * @param market The exchange to obtain data from (optional, default to )
   * @param instrument The unmapped instrument to retrieve on a specific market. (optional, default to )
   * @param instrumentStatus The status of the instrument, can be one of the following: ACTIVE, IGNORED, RETIRED, EXPIRED (optional, default to [])
   * @return Future(GENERICRESPONSE)
   */
  def indexV1MarketsInstrumentsUnmappedAsync(market: Option[String] = None, instrument: Option[String] = None, instrumentStatus: Option[List[String]] = None): Future[GENERICRESPONSE] = {
      helper.indexV1MarketsInstrumentsUnmapped(market, instrument, instrumentStatus)
  }

}

class FuturesApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def futuresV1HistoricalDays(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/days"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalDays")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalDays")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalFundingRateDays(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/funding-rate/days"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalFundingRateDays")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalFundingRateDays")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalFundingRateHours(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/funding-rate/hours"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalFundingRateHours")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalFundingRateHours")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalFundingRateMessages(market: String,
    instrument: String,
    afterTs: Option[Integer] = None,
    lastCcseq: Option[Integer] = None,
    limit: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/funding-rate-messages"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalFundingRateMessages")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalFundingRateMessages")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    afterTs match {
      case Some(param) => queryParams += "after_ts" -> param.toString
      case _ => queryParams
    }
    lastCcseq match {
      case Some(param) => queryParams += "last_ccseq" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalFundingRateMessagesHour(market: String,
    instrument: String,
    hourTs: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/funding-rate-messages/hour"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalFundingRateMessagesHour")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalFundingRateMessagesHour")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    hourTs match {
      case Some(param) => queryParams += "hour_ts" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalFundingRateMinutes(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/funding-rate/minutes"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalFundingRateMinutes")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalFundingRateMinutes")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalHours(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/hours"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalHours")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalHours")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalMinutes(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/minutes"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalMinutes")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalMinutes")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalOpenInterestDays(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/open-interest/days"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalOpenInterestDays")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalOpenInterestDays")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalOpenInterestHours(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/open-interest/hours"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalOpenInterestHours")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalOpenInterestHours")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalOpenInterestMessages(market: String,
    instrument: String,
    afterTs: Option[Integer] = None,
    lastCcseq: Option[Integer] = None,
    limit: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/open-interest-messages"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalOpenInterestMessages")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalOpenInterestMessages")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    afterTs match {
      case Some(param) => queryParams += "after_ts" -> param.toString
      case _ => queryParams
    }
    lastCcseq match {
      case Some(param) => queryParams += "last_ccseq" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalOpenInterestMessagesHour(market: String,
    instrument: String,
    hourTs: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/open-interest-messages/hour"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalOpenInterestMessagesHour")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalOpenInterestMessagesHour")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    hourTs match {
      case Some(param) => queryParams += "hour_ts" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalOpenInterestMinutes(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/open-interest/minutes"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalOpenInterestMinutes")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalOpenInterestMinutes")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalTrades(market: String,
    instrument: String,
    afterTs: Option[Integer] = None,
    lastCcseq: Option[Integer] = None,
    limit: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/trades"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalTrades")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalTrades")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    afterTs match {
      case Some(param) => queryParams += "after_ts" -> param.toString
      case _ => queryParams
    }
    lastCcseq match {
      case Some(param) => queryParams += "last_ccseq" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1HistoricalTradesHour(market: String,
    instrument: String,
    hourTs: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/historical/trades/hour"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1HistoricalTradesHour")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->futuresV1HistoricalTradesHour")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    hourTs match {
      case Some(param) => queryParams += "hour_ts" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1LatestFundingRateTick(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/latest/funding-rate/tick"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1LatestFundingRateTick")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->futuresV1LatestFundingRateTick")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1LatestInstrumentMetadata(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/latest/instrument/metadata"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1LatestInstrumentMetadata")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->futuresV1LatestInstrumentMetadata")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1LatestOpenInterestTick(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/latest/open-interest/tick"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1LatestOpenInterestTick")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->futuresV1LatestOpenInterestTick")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1LatestTick(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/latest/tick"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->futuresV1LatestTick")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->futuresV1LatestTick")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1Markets(market: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/markets"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1MarketsInstruments(market: Option[String] = None,
    instrument: Option[String] = None,
    instrumentStatus: Option[List[String]] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/markets/instruments"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }
    instrument match {
      case Some(param) => queryParams += "instrument" -> param.toString
      case _ => queryParams
    }
    instrumentStatus match {
      case Some(param) => queryParams += "instrument_status" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def futuresV1MarketsInstrumentsUnmapped(market: Option[String] = None,
    instrument: Option[String] = None,
    instrumentStatus: Option[List[String]] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/futures/v1/markets/instruments/unmapped"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }
    instrument match {
      case Some(param) => queryParams += "instrument" -> param.toString
      case _ => queryParams
    }
    instrumentStatus match {
      case Some(param) => queryParams += "instrument_status" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1HistoricalDays(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/historical/days"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1HistoricalDays")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->indexV1HistoricalDays")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1HistoricalHours(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/historical/hours"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1HistoricalHours")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->indexV1HistoricalHours")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1HistoricalMessages(market: String,
    instrument: String,
    afterTs: Option[Integer] = None,
    lastCcseq: Option[Integer] = None,
    limit: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/historical/messages"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1HistoricalMessages")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->indexV1HistoricalMessages")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    afterTs match {
      case Some(param) => queryParams += "after_ts" -> param.toString
      case _ => queryParams
    }
    lastCcseq match {
      case Some(param) => queryParams += "last_ccseq" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1HistoricalMessagesHour(market: String,
    instrument: String,
    hourTs: Option[Integer] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None,
    return404OnEmptyResponse: Option[Boolean] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/historical/messages/hour"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1HistoricalMessagesHour")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->indexV1HistoricalMessagesHour")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    hourTs match {
      case Some(param) => queryParams += "hour_ts" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }
    return404OnEmptyResponse match {
      case Some(param) => queryParams += "return_404_on_empty_response" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1HistoricalMinutes(market: String,
    instrument: String,
    groups: Option[List[String]] = None,
    limit: Option[Integer] = None,
    toTs: Option[Integer] = None,
    aggregate: Option[Integer] = None,
    fill: Option[Boolean] = None,
    mappingPriority: Option[String] = None,
    responseFormat: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/historical/minutes"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1HistoricalMinutes")

    if (instrument == null) throw new Exception("Missing required parameter 'instrument' when calling FuturesApi->indexV1HistoricalMinutes")

    queryParams += "market" -> market.toString
    queryParams += "instrument" -> instrument.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    toTs match {
      case Some(param) => queryParams += "to_ts" -> param.toString
      case _ => queryParams
    }
    aggregate match {
      case Some(param) => queryParams += "aggregate" -> param.toString
      case _ => queryParams
    }
    fill match {
      case Some(param) => queryParams += "fill" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }
    responseFormat match {
      case Some(param) => queryParams += "response_format" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1LatestInstrumentMetadata(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/latest/instrument/metadata"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1LatestInstrumentMetadata")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->indexV1LatestInstrumentMetadata")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1LatestTick(market: String,
    instruments: List[String],
    groups: Option[List[String]] = None,
    mappingPriority: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/latest/tick"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (market == null) throw new Exception("Missing required parameter 'market' when calling FuturesApi->indexV1LatestTick")

    if (instruments == null) throw new Exception("Missing required parameter 'instruments' when calling FuturesApi->indexV1LatestTick")
    queryParams += "market" -> market.toString
    queryParams += "instruments" -> instruments.toString
    groups match {
      case Some(param) => queryParams += "groups" -> param.toString
      case _ => queryParams
    }
    mappingPriority match {
      case Some(param) => queryParams += "mapping_priority" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1Markets(market: Option[String] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/markets"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1MarketsInstruments(market: Option[String] = None,
    instrument: Option[String] = None,
    instrumentStatus: Option[List[String]] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/markets/instruments"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }
    instrument match {
      case Some(param) => queryParams += "instrument" -> param.toString
      case _ => queryParams
    }
    instrumentStatus match {
      case Some(param) => queryParams += "instrument_status" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }

  def indexV1MarketsInstrumentsUnmapped(market: Option[String] = None,
    instrument: Option[String] = None,
    instrumentStatus: Option[List[String]] = None
    )(implicit reader: ClientResponseReader[GENERICRESPONSE]): Future[GENERICRESPONSE] = {
    // create path and map variables
    val path = (addFmt("/index/v1/markets/instruments/unmapped"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    market match {
      case Some(param) => queryParams += "market" -> param.toString
      case _ => queryParams
    }
    instrument match {
      case Some(param) => queryParams += "instrument" -> param.toString
      case _ => queryParams
    }
    instrumentStatus match {
      case Some(param) => queryParams += "instrument_status" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      val status = Response.Status.fromStatusCode(resp.statusCode)
      status.getFamily match {
        case Family.SUCCESSFUL | Family.REDIRECTION | Family.INFORMATIONAL => process(reader.read(resp))
        case _ => throw new ApiException(resp.statusCode, resp.statusText)
      }
    }
  }


}
